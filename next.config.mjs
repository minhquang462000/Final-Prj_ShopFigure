/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['file.hstatic.net', 'product.hstatic.net'], // Thêm các domain bạn cần
      },
};

export default nextConfig;
